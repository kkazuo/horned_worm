(jbuild_version 1)

(library
 ((name horned_worm)
  (libraries (
              batteries
              cohttp-lwt-unix
              logs
              logs.fmt
              re
              websocket-lwt.cohttp
              yojson
              ))
  (preprocess (pps (lwt.ppx)))))
